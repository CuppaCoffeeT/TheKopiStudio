Quick command to pull and push to git (/git-sync should still be the standard command whenever to push to git, only use this command if it's timesensitive and no time to run the full command)
1. Pull latest changes from git
2. Add all changes
3. Commit with a descriptive message
4. Push to git main origin