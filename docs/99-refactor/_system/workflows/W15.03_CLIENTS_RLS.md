# W15.03 — Clients domain RLS (contacts + companies)

**Created**: 2026-04-18 SGT
**Last Updated**: 2026-04-19 SGT

**Goal**: Harden client-contact + client-company tables. Most staff can SELECT (needed to build quotations), write is coordinator+.
**Tier**: Next (Week 3–4) · **Status**: 🟢 CLOSED 2026-04-19 — **no-op** (scope reconciled; client domain was already capability-gated pre-refactor) · **Automation**: hybrid
**Blocked by**: W14 ✅ · **Blocks**: nothing (feeds G4 `rls_restored`)

## Progress log

### 2026-04-19 — Scope reconciliation → closed as no-op

Live `pg_policies` scan across all 11 client-domain tables (`public.*client*` + `customer_interactions`) found **zero `ai_ro_select` / `USING(true)` holes**. Every table was already capability-gated before the refactor started. No migration applied. No Playwright run triggered. W14 ledger unchanged.

**Evidence** (verified 2026-04-19 via `mcp__supabase__execute_sql` on project `your-project-ref`):

| Table | RLS | Policies | Permissive hole? |
|---|---|---|---|
| `client_companies` | ✅ | Approved read (`is_approved_user()`) + Project roles manage (`can_manage_projects()`) | **none** |
| `client_company_emails` | ✅ | Approved read + Project roles manage | **none** |
| `client_company_log` | ✅ | Approved read + Project roles manage | **none** |
| `client_contact_log` | ✅ | Approved read + Project roles manage | **none** |
| `client_contacts` | ✅ | Approved read + Project roles manage | **none** |
| `client_types` | ✅ | Approved read + Admin manage (`is_admin()`) | **none** |
| `client_work_types` | ✅ | Approved read + Admin manage | **none** |
| `project_client_contacts` | ✅ | Approved read + Project roles manage | **none** |
| `project_client_work_types` | ✅ | Approved read + Project roles manage | **none** |
| `quotation_client_work_types` | ✅ | Approved read + Quotation roles manage (`can_manage_quotations()`) | **none** |
| `customer_interactions` | ✅ | Approved read + Project roles manage | **none** |

**Why the original card pointed at drops**: the W14 `RLS_STATE_MATRIX.md` §F line for W15.03 read "Add project_id filter in WHERE clause (already gated by capability)". That is a **tightening** design change (restrict cross-project client visibility for drafters), not a permissive-hole closure like W15.01. W15's security-hole-closure scope is only the `USING(true)` / `ai_ro_select` drops; tightening already-gated reads is out of scope.

**Follow-up**: if drafter row-scoping on clients is desired (so e.g. drafter sees only clients on their assigned projects), spawn **W15.03b** as a separate card with its own:
- Policy design (capability-based `USING` extended with `EXISTS (SELECT 1 FROM project_client_contacts pcc JOIN user assignment WHERE pcc.client_contact_id = client_contacts.id AND assigned_to = auth.uid())` — exact shape TBD)
- Playwright RLS matrix (drafter-sees-only-assigned vs coordinator-sees-all)
- Feature flag + 7-day soak (behavior change — real regression risk, unlike a permissive-policy drop)

Not creating W15.03b now; parking here until user flags this as needed.

**Matrix accuracy note**: the matrix overstated W15.03 scope in the same way it did W14's "33 RLS-off tables" (which were actually 0) — aspirational strategy text treated as literal drop list. Pattern for future W15.## inventory: always live-check `pg_policies` before trusting the matrix's strategy lines.

**Safe-rollback**: N/A — no migration was applied.

## Done-when (original — retained for history)

- Zero permissive on client_* tables — ✅ **was already zero on 2026-04-19**
- Ledger decremented — N/A (nothing to drop)
- Sets sub-flag: `rls_clients_done` — ✅ set 2026-04-19

## Related

- [W14_AUTH_RLS_AUDIT.md](W14_AUTH_RLS_AUDIT.md) — matrix source (strategy line that seeded W15.03)
- [W15_RLS_ROLLOUT.md](W15_RLS_ROLLOUT.md) — umbrella
- [W15.01_USERS_AND_PEOPLE_RLS.md](W15.01_USERS_AND_PEOPLE_RLS.md) — precedent for drop-permissive pattern
- [research/RLS_STATE_MATRIX.md](../research/RLS_STATE_MATRIX.md) — §B `client_contacts` verdict "REAL (project-gated)" matches live state
