# AppBase_REFACTOR — State

**Created**: 2026-04-16 SGT
**Last Updated**: 2026-05-31 SGT — **SRC_STRUCTURE_CLEANUP_PRD landed 2026-05-31** (see "Legacy `src/components/<domain>/` relocation" backlog row + Latest update below): `src/components/` = `{primitives,ui,shared}` only; all legacy `src/components/<domain>/` folders gone; `src/{types,constants,styles}` drained + removed; `src/hooks` root 116 → 71; OTCalculator page→feature drift edge eliminated; new `no-stray-domain-components` dep-cruiser rule (error) now enforces the shape; 105 dead files deleted · ~70 surfaces relocated/promoted. Gates: tsc 0 · drift true-0 · build pass · knip 172 → 77 · @p0 378 pass. Prior (2026-04-28): **W09 throughput: 25 module runs · MODULES_MANIFEST 26 migrated · 11 inspected · 1 audited.** Then (2026-04-28): two primitive-level a11y bugfixes shipped via interactive `/generalworks` inspection — `Drawer.tsx` now wraps title/subtitle in `VaulDrawer.Title`/`Description` (resolves "DialogContent requires DialogTitle" warning across every detail drawer); `DataTable.tsx` splits the desktop `role="table"` from the mobile `role="list"` so the wrapping role=table is fully display:none below md (resolves axe critical `aria-required-children` on every LIST adopter at mobile-safari viewport). `/generalworks` `<ListPageFrame>` testid props wired (`searchTestId` + `tableTestId`) — repairs pre-existing WF-0301 spec. Earlier (2026-04-27): W09 #24 `/auth/verify` migration + 4 specs (WF-0598..WF-0601); W09 #25-#26 `/comms` + `/comms/pending` + `/competitoranalysis` migrations; WF-0602 captured on `/auth/verified`. **Full sync** — 7 commits on origin/main (`7e22155f..e9aecda7`); W22 pre-push 35/35 @p0 green. Full history in [RECENT_CHANGES.md](RECENT_CHANGES.md).
**Status**: 🔵 Planning
**Priority**: 🔴 Critical

## Current Stage: 4_MIGRATE · S4 actively shipping (W09 25 module runs · MODULES_MANIFEST 26 migrated · 11 inspected · 1 audited · primitive a11y fixes ripple to all LIST/Drawer adopters)
## Status: 🟡 IN PROGRESS — S2 closed; S3 design-shipped; S4 module migrations in flight
## Last Updated: 2026-05-31 by weijie · SRC_STRUCTURE_CLEANUP_PRD landed — legacy `src/components/<domain>/` eliminated, `src/{types,constants,styles}` removed, `no-stray-domain-components` drift rule live

(Rolling dashboard. Individual cards each have their own 0→5 lifecycle in `workflows/W##_*.md`.)

## Backlog status (mirrors [SYSTEM_OVERVIEW.md#backlog](SYSTEM_OVERVIEW.md))

| Card | Status | Next action |
|---|---|---|
| W01 Supabase baseline | 🟢 | Closed 2026-04-18. Path B shipped; Track C Lite accepted (RPO=24h, RTO=untested, 561 MB DB on logical backups). Deferred Path A → W5. |
| W02 Module inventory | 🟢 | Closed 2026-04-18. 3 deliverables shipped (MODULE_MAP 457L · COMPONENT_MAP 690L · DUPLICATION_CLUSTERS 287L). 22 modules, 665 files, 102 clones, top W07 targets identified. User review pending. |
| W03 Workflow inventory | 🟡 | **2026-04-19 eod** — `/explore-all-modules` complete. Ledger: **144 rows · 100 green · 39 needs_skill · 1 failed** (WF-0291 quotations row-click — TableRow uses onClick not `<a href>`, selector fix). Coverage: all 47 active modules captured (WF-0276 → WF-0343). P0 still 3/19 — gate to 🟢 unchanged. Bonus: shared `ResponsiveTabsList` extended with `testidPrefix` prop (5 multi-tab modules benefit) + new `tests/pom/responsiveTabs.ts` helper. Next: P0 T2 sprint via `/write-workflow-test`. |
| W04 Multi-tool harness | 🟡 | **2026-04-19** — flipped 🔴 → 🟡. UI + DB + NAS runners live and proven (WF-0012 · WF-0019). Gmail runner pending (lands with WF-0060 Submit NCE). ghost-os runner pending. Playwright + Supabase service-role + SMB mount at `/Volumes/JLQI/` wired. Scope expanded 2026-04-18 from "Playwright seatbelt" → "5-tool evidence harness". |
| W11.02 Modules dashboard tab | 🟢 | **MVP shipped 2026-04-18** — tab live at `/refactor-dashboard` → Modules. 52 modules grouped by category + Cross-cutting virtual section. Detail pane shows per-tool evidence pills + spec links. "Describe workflow" clipboard handoff modal. Deferred to W11.03: W02 components-per-module, W05 compliance pills. |
| W11.03 Primitives adoption dashboard tab | 🟢 | **Shipped 2026-04-20.** `scripts/primitives-manifest-builder.mjs` + CLI wrapper scan `src/components/primitives/**` + grep imports across `src/**` + read latest handoff MANIFEST → produces live data. Vite dev-middleware serves at `/api/primitives-manifest`. Tab at `/refactor-dashboard → Primitives` shows table w/ preview thumbnails (iframe scaled 25%) · 🎨 Designed · 💻 Built · 🟢 Live (prod adopters only, lab/test excluded) · 🚢 adoption bar · per-row drawer w/ full iframe + Copy promote cmd button for pending primitives. Filter options include `🔴 Pending promote`. Manifest auto-regenerates on every request (stateless). **43 primitives live** (shell 11 · overlays 11 · dashboard 7 · detail 10 · atoms 3 · root 1). Replaces hand-maintained DESIGN_CATALOG.md Adopted column over time. |
| W23 Design-lab handoff staging | 🟢 | **Shipped 2026-04-19 (eod+19) · 2 live promotions today.** **W23 #1 (2026-04-26)**: staged handoff `2026-04-26-LWwN0H4g` (`component-whatsapp-thread-panel.html` · 468 LOC + `.jsx` 565 LOC + 7 chats) → promoted `<WhatsAppThreadPanel>` primitive at `src/components/primitives/detail/WhatsAppThreadPanel.tsx` (411 LOC). Pure presentation, composes `Card`+`Badge`+`Switch`+`Textarea`+`Button`+`Tooltip`. **W23 #1.1 (same day)**: polish — failed-send inline banner (red-700) + Resend link + Textarea char counter; adopted by `QuotationWhatsAppTab.tsx` (`surface="bare"`); deleted 3 orphan files in `src/components/comms/` (`WhatsAppConversation` · `WhatsAppSendBox` · `WhatsAppMessageBubble`); promoted `useWhatsAppRealtime` + new `useWhatsAppResend` to global `src/hooks/`; added `WhatsAppService.resendFailed()`. All 5 W09 hard greps zero on `src/features/people/`. **Earlier (2026-04-19)**: Vite dev-plugin (`docs-assets-handoffs`) serves snapshot files at `/docs-assets/handoffs/*` + index JSON. Route `/design-lab/handoffs` has 3-panel UI: sidebar (snapshot list + `new/changed/unchanged/removed` diff chips) · main (iframe preview) · files-panel (per-file target + "Copy promote cmd" button). Skills revised: `/design-import` STAGE writes to `docs/99-refactor/_system/design/handoffs/<YYYY-MM-DD>-<short-hash>/` + computes diff-vs-prior + MANIFEST.json + auto-archive > 5; `--promote <file>` applies mapped Edit via `/use-primitives` EDIT protocol; `/design-prompt` notes the stage→preview→promote loop. `tsc --noEmit` + `npm run build` green. |
| W22 CI gates | 🟢 | **Closed 2026-04-19 (eod+13) · all 6 structural done-when items met.** Hardening shipped: (1) `.github/workflows/seatbelt.yml` sync-ledger job auto-updates `Last CI green` on every green run; (2) pre-commit restores `lint-staged` (ESLint on staged files); (3) pre-push + CI run full `npm run lint` with `--max-warnings=1610` (pragma ceiling against pre-existing debt); (4) `assertNasMountAvailable()` auto-skips WF-0012+WF-0019 on CI when no SMB mount, still hard-fails locally; (5) `SMOKE_MAX_ROUTES=20` caps WF-9000 on ubuntu-latest; (6) WF-0019 re-added to `@p0` with 3/2s/5s/15s NAS retry. Flag `ci_enforced` ✅. Zero-bypass week tracked observationally — 6 of today's SKIP_E2E=1 uses all had documented reason (5 docs-only + 1 agent-collision); formal observation starts from this flip. Gmail-sync 8.66% 5xx rate noted in verification report — handed off to W04 Phase 5b as its own observability card, not a W22 blocker. |
| W05 Drift detector | 🟡 | **⏰ Next check: Fri 2026-04-24 — run `bash scripts/drift_weekly_digest.sh`.** If the digest shows non-zero file churn + rule-count delta vs baseline, flip W05 🟢. Until then, 🟡. <br>**Done 2026-04-19**: tag `pre-refactor-baseline` on origin at `9201024` · dependency-cruiser 17.3.10 + 4-rule config (`.dependency-cruiser.cjs`) + `drift:check`/`drift:graph` scripts · baseline frozen (992 modules, 5 circular-dep errors, 0 cross-feature leaks, 0 page→feature imports) · 16 non-`ui/` oversharing signals handed to W07 · [W05_DRIFT_BASELINE.md](research/W05_DRIFT_BASELINE.md) + [first digest](research/W05_DRIFT_WEEKLY_2026-04-19.md) · weekly digest script manual-only per card guardrail. <br>**Deferred (not W05)**: CI PR-comment wiring → W22; 5 circular deps → self-resolve under W09 module moves; X8 closes only when W05 + W20 both 🟢 (W18 + W19 already 🟢). |
| W06 Dead code purge | 🟢 | Closed 2026-04-18. 68 files removed (8 dead + 60 knip-verified orphans), 3 singleton folder moves, ~18,870 LOC gone, tsc clean. DB column audit agent was 8/8 wrong — zero signal; column cleanup deferred to W09 per-module migration. |
| W07 Shared primitives | 🟡 | **Phase 2 ✅ — 70/70 primitives designed + built · adoption-gated on W09 (2026-04-20 eod+2).** Day delta: 44 → 70. New today: **form/** kit (11 · Input · Textarea · Select · Checkbox · Radio · Switch · DatePicker · FileUpload · Field · Progress · Stepper), **ui/** table kit (9 · DataTable · TableHeader · DataRow · SortIcon · TableCheckbox · Pagination · PageBtn · MobileListCard), **charts/** kit (8 · ChartShell · AreaChart · BarChart · HBarChart · ChartTooltip · ChartLoading · ChartError · LegendRow), **shell/** FilterPill. Built by 3 parallel Explore-agents from handoff `2026-04-20-nl73fwyg` JSX (~1800 LOC), each .tsx independently typecheck-verified + path-verified (one earlier agent hallucinated paths; this batch didn't). AppHeader redesign from `FmPJtwZw`: pixel lockup · Bell-first right order · 6px gap · glass 72/70 · mobile back-chevron. Builder fix: sort handoffs by MANIFEST `staged_at` not folder name (fixed same-day collision). Folder-level MANIFEST targets (`overlays/ shell/ dashboard/ detail/ ui/ form/ charts/`) surface every .tsx as `designed:true` in the dashboard. `tsc` + `build` green. **0 pending promote**. Current adoption: 44 (unchanged — new primitives lab-only until W09 consumes). Earlier: S4a Heavyweight Detail 10 primitives + DetailPageFrame wrapper (2026-04-20 eod). Earlier: Today: 9 detail primitives promoted from Claude Design handoff `2026-04-20-MUmgnpT1` (+ LineItemsEditor from `2026-04-20-iV8W8LYb` with split product code/name/description cols) + new **`<DetailPageFrame>` wrapper** that internally composes AppHeader + PageShell + TabNav — detail pages now import ONE component with flat props (breadcrumb · title · status · actions · tabs · sideRail · children), zero chrome plumbing. Same shim pattern as `DashboardHeader` — if inner API changes, ONLY wrapper updates, zero page edits across all 12 detail routes. Visual regression lab at `/design-lab/heavyweight-detail`. **36 → 45 primitives**: shell(11) · overlays(11) · dashboard(7) · **detail(10 NEW)** · atoms(3) · cells(4 — counted under shell). Earlier: `DashboardHeader` shim over AppHeader lets 71 list pages inherit glass chrome with zero call-site edits. Phase 2 remaining 🔴: Stepper · MobileDrawer (S6) · WorkflowProgressBar · ProgressCard (S10) · MapCanvas · SpatialPicker · DrawingModal (S11) · IntegrationCard (S12) · KpiTile · Chart (role-dashboards session). Governance: [UNIVERSAL_COMPONENTS.md](UNIVERSAL_COMPONENTS.md) + [.claude/rules/universal-components.md](../../.claude/rules/universal-components.md) + `/use-primitives` skill + [primitives/CONTEXT.md](../../src/components/primitives/CONTEXT.md). Full inventory live at **[/refactor-dashboard → Primitives](http://localhost:8080/refactor-dashboard)**. Shim rewrites `src/components/DashboardHeader.tsx` to delegate to `<AppHeader>` + `<ImpersonationBanner>` + page-heading block; 71 adopter pages inherit the glass S-shell chrome with zero call-site edits. Direct per-page migration deferred until S4 Detail lands `<PageShell>` — at that point becomes 5-min find-replace. shell(11): AppHeader · Breadcrumb · ImpersonationBanner · Button · Chip · FilterBar · **SearchInput (new, extracted from FilterBar)** · FloatingCTA · LoadingSkeleton · ErrorState · NoResultsState. cells(4): **DateCell · DateTimeCell · CurrencyCell · NumberCell** (new — wrap timezoneUtils/currencyHelper for DataTable columns). overlays(11): Modal · Drawer · Popover (+`withArrow`) · Tooltip (+`shortcut`) · DropdownMenu · ContextMenu · Alert · Toaster (root-mounted, AppBaseToast custom render, glass-95%) · SearchableMultiSelect · Kbd · CommandPalette (⌘K). dashboard(7): GreetingHeader · ModuleCard · NeedsAttentionPill · AttentionHeader · CategoryHeader · ModuleSearch · CountBadge. atoms(3): Avatar · IconButton · StatusBadge. **Phase 2 infra batch (eod+15i)**: typed Supabase client at [`src/lib/supabase/typed-client.ts`](../../src/lib/supabase/typed-client.ts) (`fetchPage`/`fetchOne`/`fetchDropdown`/`fetchCount` force `.range`/`.single`/`.limit(5000)`/`{head:true}` terminators); vitest + @testing-library + jsdom + jest-dom installed, `npm test`/`test:run`/`test:ui` scripts live, 4 Phase 1 primitive test files run green (22/22). W07 Phase 2 chore items 4/5/6/7 all ✅ shipped. **All 9 overlays + list atoms screenshot-verified vs spec at [`/design-lab/overlays`](../../src/features/design-lab/overlays/OverlaysLabPage.tsx).** `@/components/ui/sonner` is a shim → 100+ legacy toast callers auto-benefit. Remaining Phase 2 🔴 **BLOCKED** on S4+: PageShell · Timeline (S4) · LineItemsEditor (S5) · Stepper · MobileDrawer (S6) · WorkflowProgressBar · ProgressCard (S10) · MapCanvas · SpatialPicker · DrawingModal (S11) · IntegrationCard (S12) · KpiTile · Chart (role-dashboards session). Governance: [UNIVERSAL_COMPONENTS.md](UNIVERSAL_COMPONENTS.md) + [.claude/rules/universal-components.md](../../.claude/rules/universal-components.md) + `/use-primitives` skill + [primitives/CONTEXT.md](../../src/components/primitives/CONTEXT.md). Full inventory in [DESIGN_CATALOG.md](DESIGN_CATALOG.md). |
| W08 Design system | 🟡 | **Phase 3 effectively COMPLETE — 70/70 primitives designed; targeted pivot delivered all 3 micro-items + full form/table/chart kits + AppHeader v2 on 2026-04-20 eod+2.** All 15 session equivalents now 🟢 or ⚫ deferred. **🟢 delivered**: S1 · S2 · S-shell · S-shell-v2 (AppHeader) · S3 · S3b charts · S4a · S5 · S6 form kit · S9 · S10 bar · S1b DataTable kit. **⚫ deferred** (no forcing W09 page blocked): S4b · S4c medium/light detail (reuse S4a kit) · S7 Settings · S8 Tool (composition-only) · S11 Spatial · S12 Integration · KpiTile (user pick: use charts + StatusBadge compositions). W08 stays 🟡 only because adoption lags — flips 🟢 when W09 consumes end-to-end (≥5 page migrations with visual-verify green). No more full archetype sessions unless a W09 page blocks. Earlier: Next focused work (user-requested · not full Claude Design sessions): (1) **Promote Stepper** — HTML preview already in handoff `2026-04-20-Q32ToUrn/component-stepper.html`, promote to `src/components/primitives/detail/` or `form/` (~15 min). (2) **Mini chat: S10 Progress** — design WorkflowProgressBar + ProgressCard + CDW step strip (~1hr chat). (3) **Mini chat: S6 Form FormRows** — design FormRow + FormField + FormSection (~1hr). MobileDrawer already covered by existing `Drawer` primitive in overlays. After these 3 micro-items, focus shifts fully to W09 migrations. **Phase 3 delivered earlier Phase 1 ✅ (Tailwind v4 + tokens). Phase 2 ✅ (app activation + font + CTA slate-800 + 5556-site audit). **Phase 3 delivered**: S1 List/Table ✅ · S2 Overlays ✅ · S-shell ✅ · S3 Dashboard ✅ · **S4a Heavyweight Detail ✅** (10 primitives built + DetailPageFrame wrapper · visual-verified at `/design-lab/heavyweight-detail` · pre-adoption, 0/5 pages migrated). WAVE 1 (shared primitives) shipped · WAVE 2 (5 parallel per-page chats) is NEXT — each chat scoped to ONLY the 3-ish page-specific atoms + DetailPageFrame composition, with CDW+Spatial explicitly deferred to S11 Spatial session. **Queued**: S4b Medium detail (3 pages) · S4c Light detail (5 pages) · S6 Form · S7 Settings · S8 Tool · S9 Atom polish · S10 Progress · S11 Spatial · S12 Integration · S3b Role dashboards. **Visual verification**: `.claude/rules/design-system.md` + `/use-primitives` skill Mode B + 3 regression labs (`/design-lab/overlays` · `/design-lab/heavyweight-detail` · `/design-lab/handoffs`). **W08 stays 🟡 until W09 consumes the audit.** Plan: [CLAUDE_DESIGN_GAME_PLAN.md](CLAUDE_DESIGN_GAME_PLAN.md) · [DESIGN_CATALOG.md](DESIGN_CATALOG.md) · live inventory at [/refactor-dashboard → Primitives](http://localhost:8080/refactor-dashboard). |
| W09 Per-module migration | 🟡 | **25 module runs (2026-04-20 → 2026-04-28) · MODULES_MANIFEST: 26 migrated · 11 inspected · 1 audited · 0 production-ready.** Earlier list: dashboard · companylist+detail · serviceslist · clientprofiles+contactdetail · payslip · payment-management (shell + 3 sub-tabs) · projectlist · generalworks · productsservices · xero-settings · emailinbox · quotation-list · quotation-settings · quotation-create · peoplemanagement-list · peoplemanagement-detail · quotationdetail. **2026-04-27**: W09 #24 `/auth/verify` (288 LOC monolith → dispatcher: 16 LOC page + 117 LOC hook + 3 state components; AUTH_EMAIL_VERIFICATION sub-guide; 4 Playwright specs WF-0598..WF-0601) · W09 #25 `/comms` (181 LOC → 1 page 50 LOC + 4 components ≤84 LOC + 3 hooks ≤38 LOC; new `invalidateEntity.whatsapp` helper; pending-count via `fetchCount`) · W09 #25 `/competitoranalysis` (feature-folder shape) · W09 #26 `/comms/pending` (363 LOC → 6 components + 3 hooks + page 108 LOC; `Card`+`Modal`+`Badge`+`Button.loading`). **2026-04-28**: `/generalworks` `<ListPageFrame>` testid wiring (`searchTestId` + `tableTestId`) — repairs WF-0301; surfaced via interactive smoke that also drove the Drawer + DataTable primitive a11y bugfixes (see "Cross-cutting primitive bugfixes" row). **11 inspected**: clientprofiles · comms · comms-pending · companydetail · companylist · contactdetail · productsservices · quotationdetail · quotations · quotations-create · quotationsettings. **Next up**: invoice-detail · project-detail (parallels W13 split) · `/sync-module-documents quotationdetail --inspected "..."` + `/workflow-audit quotationdetail` → production-ready flip. |
| W07 a11y bugfixes (cross-cutting) | 🟢 | **2026-04-28 · primitive-ripple fixes from /generalworks inspection.** (1) `Drawer.tsx`: title/subtitle now use `VaulDrawer.Title`/`Description` instead of raw `<div>`s — resolves "DialogContent requires DialogTitle" Radix warning surfaced every time any detail drawer opens; affects every adopter incl. WhatsAppThreadPanel, GeneralWorksDetailDrawer, future detail-page drawers. (2) `DataTable.tsx`: split `role="table"` from `role="list"` mobile body so role=table only wraps the desktop subtree (with `hidden md:block` on the wrapper itself) — resolves axe critical `aria-required-children` on every LIST adopter at mobile-safari viewport (was tripping WF-0310 with 1 violation per axe scan). Both shipped under `e9aecda7`; W22 @p0 35/35 green. |
| W10 Scaffolding skills | 🔴 | Blocked: W07 |
| W11 Refactor dashboard | 🟢 | Closed 2026-04-18 (Phase 1 complete, user-approved). Phase 2–4 deferred to W11.02/.03/.04 (blocked by W02 / W07+W17 / W08). |
| W12 Compliance sweeps | 🟢 | Closed 2026-04-18 for Sweep 1 LEGACY-FIX scope (153 fixes, 51 files, tsc clean). **Re-scoped (Option A)** — behavior-changing work split into W12.02 (PAGINATE) · W12.03 (RBAC) · W12.04 (timezone) · W12.05 (HITL dynamic) — all blocked by W04. `sweeps_done` flag = Sweep 1 mechanical done. |
| W12.02 Sweep 1b PAGINATE | 🟡 | **Part A 🟢 shipped 2026-04-19 (eod+15) · commit `09c68ad`.** `.limit()` safety net across 12 unbounded list queries (11 files). Audit rescoped: 29 claimed → 2 deleted in W06 + 7 false positives + 12 legacy-limit fixed (Part A ✅) + 0 pages that need UI-pagination right now. `.limit(5000)` on catalogue fetches, `.limit(10000)` on role-scoped work queues. @p0 suite 18/18 green. **Part B ⏸ deferred to post-W07 Phase 2** — full `useURLPagination` + `.range()` migration blocks on W07 `<DataTable>` primitive (itself blocked on W08 S1 List/Table). |
| W12.03 Sweep 2 RBAC | 🟢 | **Closed 2026-04-19 (eod+12).** W12.03 (6 orphan gates → `/admin` module) + W12.03b (4 live gates → `hasCapability()`) both done. W12.03b: `ProjectDetailPage.isAdmin` → `finance` cap · `PeopleManagement.isSuperAdmin` → new `change_user_role` cap (prop renamed `canChangeRole`) · `XeroSettingsPage.isSuperAdmin` → new `manage_integrations` cap (var renamed `canManageConnection`) · `OTEditModeToggle.canEdit` → new `edit_ot_entries` cap. Migration `w12_03b_add_ui_capabilities` applied via Supabase MCP (5 rows, 3 new capability slugs). AuthContext extended (+`capabilities: string[]`, +`hasCapability(cap)`, +impersonation parity); no `useAuth(requiredRoles?)` signature change. tsc clean; Playwright touched-modules 13/13 green post-retry (sole flake WF-0314 transient login). 2 Index.tsx sites remain deferred to W09 auth migration (not blocking closure). **Server/UI drift flag** → `users_mgmt_write` RLS allows `management` + `super_admin` but UI now limits role changes to `super_admin` via `change_user_role` cap — tighten server in W15.01 follow-up. Audit: [research/W12_03_SITES.md](research/W12_03_SITES.md). |
| W12.04 Sweep 3 Timezone | 🟢 | **Closed 2026-04-19 · commits `58fd0ce → 01919bb` (10 batches).** 332/352 raw `format()` calls (94%) + all 9 `parseISO` sites swapped across 155 files. Wrapper-first approach: batch 0 extended `timezoneUtils` with 8 new SGT-safe wrappers (formatDisplayDateLong/TimeLong, Slashed/TimeSlashed, US/TimeUS, MonthYear, parseFromDatabase), lifting auto-swap from 45% → 94%. Batch 1b collapsed `useWorkerOTSummary`'s toLocaleString wall-clock hack. 20 NEEDS-HUMAN tail: PPP/LLL shadcn pickers, payslip PDF `dd-MMM-yy`, filename `MMM_yyyy`, local-tz form inputs (`datetime-picker.tsx`). Rule [.claude/rules/timezone.md](../../../.claude/rules/timezone.md) updated with allowlist for arithmetic/comparison raw-use. Every commit tsc-clean (HEAD baseline 791, delta 0). Playwright 6/6 @p0 + 55/56 touched-module specs green. Manual visual on /quotations · /payslip · /ot-calculator · /generalworks · /emaillogs clean. Sub-flag `sweep_3_timezone_done` ✅ (feeds G4). Triage: [research/W12_04_TRIAGE.md](research/W12_04_TRIAGE.md). |
| W12.05 Sweep 1c HITL dynamic | 🟢 | **Closed 2026-04-19 · commit `2e4d960`.** Audited 250 unbounded reads (fresh grep, post-Sweep-1). Auto-triage + HITL: 209 LEGACY-FIX `.limit(10000)` + 1 DROPDOWN `.limit(5000)` + 18 ALREADY-OK (false positives from narrow classifier window) + 22 PAGINATE confirmed 1:1 into W12.02 scope. 0 NEEDS-HUMAN remaining. Playwright 216/218 green (2 flaky: WF-0012 passes on retry · WF-0019 SMB NAS teardown race — neither a code regression). Sets `sweep_1c_hitl_done` ✅. Triage: [research/W12_05_TRIAGE.md](research/W12_05_TRIAGE.md). |
| W13 ProjectDetailPage split | 🔴 | Blocked: W04 (seatbelt) + W07 (primitives). S4 proof-of-recipe before W09 loop |
| W14 Auth + RLS audit | 🟢 | Closed 2026-04-18. **Live state**: 130 tables, 0 RLS-off, 20 permissive remaining (after COMMENT ON POLICY tagging). X12 ✅ capability-based. Watchdog SQL handed to W20. Unblocks W15. (Note: agent matrix claimed 33 RLS-off — wrong; corrected inline in card.) |
| W15 RLS per-domain rollout | 🔴 | Umbrella — unblocked by W14 ✅. Spawns 5 sub-cards W15.01–.05, HIGH-risk first. Each sub-card must decrement the permissive-policy ledger + pass Playwright RLS matrix. Gates G4 `rls_restored`. |
| W15.01 Users + people RLS | 🟢 | **Closed 2026-04-19 (eod+13) — early flip, zero consumer impact.** Full identity domain clean: `users` · `workers` · `user_modules` · `role_modules` `ai_ro_select` dropped (migrations `w15_01_*` + `w15_01b_*`). Verification (Mac Mini + Supabase mgmt API): 0 `permission denied` on ai_readonly across 30h logs · 0 live sessions as ai_readonly in pg_stat_activity · 0 pg_cron jobs using the role · zero consumer references in `/Volumes/YourVolume/**` + AppBase repo. Role is orphaned — never connected. All production consumers (Mac Mini crons, edge functions, Xero sync) use `service_role` (bypasses RLS entirely). 7-day soak rule waived: no population to observe. Ledger 20 → 16. `people` policies verified capability-based (5 live, all `is_approved_user()` / `can_manage_projects()` etc). |
| W15.02 Payroll + salary RLS | 🟡 | **Part A 🟢 shipped 2026-04-19 (eod+14)** — plugged `worker_applications` `SELECT: true` PII leak (→ `has_module('HR Applications') OR is_admin()`). Migration `20260419_165635_w15_02_worker_applications_rls.sql`. Seatbelt `tests/workflows/rls/worker-applications-rls.spec.ts @p0` green via SECURITY DEFINER `w15_02_policy_audit` helper. Ledger 14 → 13. **Part B ⏸ deferred to post-W17** (needs multi-role test users before regressive tightenings on `worker_leave_balance` / `worker_ot` / worker-self-read on salary+pay_slips). |
| W15.03 Clients RLS | 🟢 | **Closed 2026-04-19 — no-op.** Live `pg_policies` scan of 11 client-domain tables found zero permissive holes; client domain was already capability-gated pre-refactor. Matrix's W15.03 line was aspirational tightening, not hole-closure. Ledger unchanged. If drafter row-scoping desired, spawn W15.03b. Sets sub-flag `rls_clients_done` ✅. |
| W15.04 Projects + quotations RLS | 🟢 | **Closed 2026-04-19 · migration `20260419_160656_w15_04_projects_quotations_rls.sql` via Supabase MCP.** Dropped `projects.ai_ro_select` (permissive → role `ai_readonly`; capability-gated `is_approved_user()` read already existed) + replaced `quotation_lifecycle_events.auth read lifecycle events` with `quotation_lifecycle_events_select_approved` (`is_approved_user()`). Service-role INSERT on lifecycle events kept (intentional — Python lifecycle agent). Playwright @p0 8/8 · tsc 0 errors · build clean. Ledger 16 → 14 (and concurrent W15.05 drop → 2). Sub-flag `rls_projects_and_quotations_done` ✅. |
| W15.05 Ops + logs RLS | 🟢 | **Closed 2026-04-19 (eod+14).** Migration `20260419_160554_w15_05_ops_logs_rls.sql` dropped 12 permissive policies via Supabase MCP. 8 were DROP-only (real capability-gated policies already behind the permissive ones): email_classifications, email_threads, modules, roles, services, trial_trenches, trial_trench_services, trial_trench_status_log. 4 got new replacements (`is_approved_user` read + `is_admin` write/delete) in the same migration: site_form_pdf_templates, whatsapp_threads, whatsapp_messages, whatsapp_participants. No new capability slugs needed. service_role bypasses RLS → Mac Mini Baileys daemon + email crons unaffected. Supabase logs: 0 permission-denied post-migration. tsc + build clean. Ledger 14 → 2 (W15.04 landed in parallel; only W15.02 payroll remains — last hole before G4). |
| W16 MFA / 2FA | 🔴 | Uses Supabase Auth built-in MFA. Mandatory for admin + payroll, opt-in for rest. S4 card |
| W17 Component library | 🟢 | **CLOSED 2026-04-19 (eod+3) · X6 ✅ COMMITTED.** All 7 real-choice slots locked + 3 cross-cutting patterns (CTA · glass · mobile data-table). Picks: Card=shadcnblocks-clean · DataTable=TanStack+Motion · KPI=Tremor+NumberTicker · Drawer=vaul+Motion handle · Stepper=Animated chip · Timeline=scroll-beam · Charts=Tremor+Motion entrance. Patterns: primary CTA=`zinc-700` (NOT red — Linear/Vercel-style); brand red kept as ACCENT only (status badges, notification dots, picker selection, focus rings); sticky glass header on every page; subtle gradient page backdrop; optional translucent Card variant for marketing/hero surfaces. 3 mockups proved composition (Quotation List · SupervisorWorkEntry mobile · EOS Dashboard). Full handoff to W07/W08 in [LOCKED_PICKS.md](LOCKED_PICKS.md). DESIGN_SYSTEM.md superseded. Sets DAG flag **`components_chosen`** ✅ (G2 prerequisite). |
| W18 Docs audit + archive | 🟢 | **Closed 2026-04-18 with scope reduced.** 13 docs archived (openclaw + old email + deprecated pages) + 6 UPDATE link fixes. Standalone 167-doc audit abandoned after agent keyword-scan failures (11/25 over-archive; 4/30 "unclear" archive verdicts with no grep evidence). Per-module doc rewrites fold into W09; over-budget splits fold into W19. Residual 30 NEEDS-HITL handled when parent module reaches W09. |
| W19 MWP context architecture | 🟢 | **Closed 2026-04-19.** All 3 tracks shipped: Track 2 — `docs/99-meta/TOKEN_BUDGET.md` (5.6K, registered + indexed); Track 1 — 14 workspaces (6 new + 8 verified) covered by CONTEXT.md within budget; Track 3 — 3 workspaces seeded with `decisions.md` + `lessons.md` (`src/features/refactor-dashboard/` · `supabase/migrations/` · `tests/workflows/`) using verified history. Root CLAUDE.md gained 2 always-loaded rule lines (memory-reading + budget link). Sets flag **`context_refreshed`** ✅ (G2 prerequisite). Enforcement is `/check-docs` flag-only today; CI enforcement folds into W22 per Q-W19-e. |
| W20 Claude-powered cron watchdog | 🔴 | **Week 5.** Mac Mini cron registered via ADD_JOB.md REST API; runs every 2h; auto-fix low-risk / escalate high-risk via Telegram |
| W21 React Query cache fix | 🟢 | **Formal CLOSED 2026-04-19 (eod+14).** All 4 tasks shipped earlier in the day (W21-1 `28dedce` · W21-2 `f6b513f` · W21-3 `016219b` · W21-4 `e38d94e`). Cache-staleness Playwright gate landed in commit `95e2959` as WF-0099 (`tests/workflows/quotation/cache-staleness-edit-back.spec.ts @p0`) — seeds quotation, edits project_name, navigates back, asserts fresh name without manual refresh. 16/16 green on chromium-desktop + mobile-safari in the @p0 suite. Deferred in card: ~33 component inlines (W07/W09) · onSuccess-required ESLint rule (custom plugin). |
| Legacy `src/components/<domain>/` relocation (SRC_STRUCTURE_CLEANUP_PRD) | 🟢 | **DONE 2026-05-31.** `src/components/` = `{primitives,ui,shared}` only; all legacy `src/components/<domain>/` folders gone; `src/{types,constants,styles}` drained + removed; `src/hooks` root 116 → 71; OTCalculator page→feature drift edge eliminated; new `no-stray-domain-components` dep-cruiser rule (error) now enforces the shape. 105 dead files deleted · ~70 surfaces relocated/promoted. Gates: tsc 0 · drift true-0 · build pass · knip 172 → 77 · @p0 378 pass. Closes the W06/W09 folder-shape drift backlog the refactor program never had an explicit card for. |

## Decided strategies (stable — change via feedback loop only)

- Refactor in-place (not fork) — see [RAW_REQUIREMENTS.md](RAW_REQUIREMENTS.md) constraints
- Seatbelt-first — no W09 migration merges without W04 coverage on the module's P0 workflows
- Markdown is canonical — `/refactor-status` page (W11) reads this file, never writes to it

## Blockers

- _None blocking._ W01 closed, W04/W09 unblocked (caveat: W04 runs against prod with `is_test_data=true`).
- **Manual cosmetic**: user to toggle OFF "Automatic branching" in Dashboard → Project Settings → Integrations → GitHub Integration. Non-blocking.
- **Week 1 cards** (W02, W06, W11, W12, W14, W18) — all unblocked, can start now.

## Latest update

**2026-05-31** — **SRC_STRUCTURE_CLEANUP_PRD landed — `src/components/` is now `{primitives,ui,shared}` only.** All legacy `src/components/<domain>/` folders eliminated; the root `src/{types,constants,styles}` directories drained of live code and removed; `src/hooks` root trimmed 116 → 71 (feature-scoped hooks pushed into their `features/<domain>/hooks/`); the last OTCalculator page→feature drift edge eliminated. A new `no-stray-domain-components` dependency-cruiser rule (severity **error**) now fails the build if any non-`{primitives,ui,shared}` folder reappears under `src/components/` — the shape can't drift back. Net: **105 dead files deleted · ~70 surfaces relocated/promoted**. Gate scorecard: `tsc --noEmit` **0** · drift **true-0** · `npm run build` **pass** · knip **172 → 77** · @p0 suite **378 pass**. Closes the folder-shape drift the refactor program had been carrying since W06/W09 without an explicit card. Full bullets in [RECENT_CHANGES.md](RECENT_CHANGES.md).

**2026-04-28** — **Drawer + DataTable primitive a11y fixes ripple to every adopter · /generalworks testid wiring repairs WF-0301 · git-sync brought local up to origin/main.** Two real a11y bugs surfaced via interactive `/generalworks` inspection (seeded row → list → drawer → 3 tabs → edit modal → every text/textarea field, with strict console-error gate). (1) `Drawer.tsx` header rendered title/subtitle as raw `<div>`s; vaul (Radix Dialog under the hood) requires `Drawer.Title` for screen readers. Wrapped in `VaulDrawer.Title` + `VaulDrawer.Description` — every detail-drawer adopter now passes the Radix a11y check. (2) `DataTable.tsx` wrapped two `hidden md:block` rowgroups + a `role="list"` mobile body inside one `role="table"` div; on mobile-safari (390px) axe saw role=table with no `row`/`rowgroup` visible children → critical `aria-required-children` violation. Restructured: role=table only wraps the desktop subtree (with `hidden md:block` on the wrapper); role=list is a sibling. Below md the role=table is fully display:none, axe skips it. WF-0310 chromium-desktop + mobile-safari both green; W22 @p0 35/35 (9.9 min). `/generalworks` `<ListPageFrame>` testid props wired (`searchTestId` + `tableTestId`) — repairs pre-existing WF-0301. Pushed as `e9aecda7`. Earlier (2026-04-27): W09 #24 `/auth/verify` (`78e2a341`); W09 #25-#26 `/comms` + `/comms/pending` + `/competitoranalysis` (`5949222a`); WF-0602 `/auth/verified`. **MODULES_MANIFEST**: 26 migrated · 11 inspected · 1 audited · 0 production-ready (Sky added 5 inspections today: comms · comms-pending · companydetail · companylist · contactdetail).

**2026-04-27** — **`/quotations/:id` reaches 100% primitive-composed (modulo sanctioned exceptions).** Three landings closed the quotation-detail thread end-to-end: (1) **W23 #2 + #3** — 2 design handoffs staged + promoted: handoff `2026-04-26-nXICiIpw` → `<QuotationFollowUpCard>` (chase-trail card) + `<CompetitorPricingCard>` (DataTable inline-edit + stats strip); handoff `2026-04-27-dt2ISkQ` → `<SpatialFeatureSelector>` decomposed to orchestrator + `spatial/SpatialFeatureMap` + `lib/spatialGeometry.ts` (Turf helpers + 60+ part colors). All Leaflet/Turf/OneMap behavior preserved (multi-layer click cycling · auto-zoom triggers · keyboard nav · batched cross-quotation refs · part-color sync with backend PDF legend). (2) **W09 quotation-detail-body BIG migration** — 9 sibling files relocated from `src/components/quotation/{enhanced,cdw-parts}/` into `src/features/quotations/components/`. Body monolith (`UnifiedQuotationView.tsx` 2,544 LOC) now lives in feature folder. Legacy subfolders deleted. (3) **W09 quotation-detail-cleanup + sanctioned-list aggressive swap** — closed every deferred row: token sweep 116 → 0 (gray→zinc · yellow→amber · indigo→info · `bg-gray-800` → primitive `Button`) · raw `<label>` 10 → 0 (3 swapped to `<span>` · 7 Switch labels swapped to `<span id>` + `aria-labelledby`) · raw `<button>` 6 → 0 (LinkedEmailThreadCard CollapsibleTriggers ×2 → native `CollapsibleTrigger` · AgentTaskRow accordion → primitive `Collapsible` · Unlink → `Button`) · `ui/searchable-select` ×4 → primitive `SearchableMultiSelect` · `ui/date-picker` → primitive `DatePicker` · `ui/calendar` removed · 2 `AlertDialog` → `Modal`. **Primitive enhancement**: `DatePicker.disabledDate?: (d) => boolean` prop shipped (additive, backward-compat; cells render `line-through` + `cursor-not-allowed` when callback returns true). Final feature-folder hard-grep state: 6a/6d/6e zero · 6b 6 sanctioned (cross-feature deferrals per Bulletproof React) · 6c 4 sanctioned (ProductPicker bespoke listbox ×3 + RHF form-array `<input>`). `tsc --noEmit` + `npm run build` (10.40s) green; primitives manifest **104 primitives · 900 adoptions** (+6 from this session); depcruise zero violations on `src/features/quotations/` (43 modules · 52 deps). Governance updated: `lib/NOTES.md` closes deferral table · `DESIGN_CATALOG.md` 🟡 P2 → 🟢 P3 BODY DECOMPOSED on the `/quotations/:id` row · `MODULE_NOTES.json` `quotationdetail` slug carries 4 dated entries · `decisions.md` appended · all 3 handoff MANIFESTs flipped `staged → promoted`. **Next**: user-invoke `/sync-module-documents quotationdetail --inspected "..."` + `/workflow-audit quotationdetail` to flip production-ready.

**2026-04-26** — **W09 #18 PersonDetail migrated · W23 #1 + #1.1 `WhatsAppThreadPanel` shipped + adopted · `comms/` folder deleted.** Three landings closed today's PersonDetail thread end-to-end: (1) `/peoplemanagement/:id` migrated to `<DetailPageFrame>` + 13 components + 9 hooks decomposed under `src/features/people/`. Single-caller orphan-sweep deleted `comms/PersonWhatsAppPanel`; relocated `services/workerEmploymentService` → `features/people/api/`. Five W09 hard greps zero. (2) Same day `/design-import` staged `2026-04-26-LWwN0H4g` from Claude Design → promoted `<WhatsAppThreadPanel>` primitive (411 LOC) at `src/components/primitives/detail/`. Pure presentation; caller wires data + mutations. Variants `surface=card|bare`, `compact`. Adopted by `PersonWhatsAppSection` — closed the W15 deferral that W09 #18 had logged earlier in the day. (3) W23 #1.1 polish: failed-send inline banner with Resend link + Textarea char counter (`{n}/1024`). `QuotationWhatsAppTab.tsx` rewritten to use `<WhatsAppThreadPanel surface="bare">`; promoted `useWhatsAppRealtime` + new `useWhatsAppResend` to `src/hooks/` (multi-caller); added `WhatsAppService.resendFailed()`. Orphan-deleted 3 files (`comms/{WhatsAppConversation,WhatsAppSendBox,WhatsAppMessageBubble}.tsx`) + empty `src/components/comms/` directory. `tsc --noEmit` clean; `npm run build` green in 10.98s. Governance updated: `primitives/CONTEXT.md` 107 → 108 primitives; `DESIGN_CATALOG.md` section N added (105 primitives across 13 groups); `universal-components.md` Need→Import row added; `legacy-replacement.md` flipped 2 rows from "TODO" → shipped. `MODULES_MANIFEST.json` rebuilt — 85 modules · 17 migrated · 0 prod-ready (`persondetail` row scaffolded with `migration.status: migrated`, `uses_detail_page_frame: true`, captured `WF-0480 → WF-0498` (20) / tested 1). `/sync-module-documents persondetail` ran with TOKEN_BUDGET sweep — `features/people/CONTEXT.md` 4.8× over 1,600c ceiling (flagged for user; recommend exemption since the doc is structural one-row-per-file). Still pending user-invocation: `/align-module-docs persondetail` + `/check-a11y persondetail` + `/check-mobile persondetail` + `/check-drift persondetail` + remaining 19 Playwright spec writes.

**2026-04-20** — **`DashboardHeader` shim ships glass chrome to 71 pages · direct migration deferred to post-S4.** Rewrote [src/components/DashboardHeader.tsx](../../src/components/DashboardHeader.tsx) to delegate to `<AppHeader>` + `<ImpersonationBanner>` + a page-heading block (h1 Geist Pixel Square · description · back button). Extracted the shared auth+theme+sign-out chrome state into [src/hooks/useDashboardChrome.tsx](../../src/hooks/useDashboardChrome.tsx) so future direct-import callers don't each re-derive it. Effect: every one of the 71 `import DashboardHeader from '@/components/DashboardHeader'` call-sites now renders the primitive chrome with zero edits per page. Pre-existing legacy visuals replaced across the app: amber impersonation banner → red-50 `<ImpersonationBanner>` with pulsing dot + ⌘⇧I hint; gray-50 page bg → `var(--page-bg)`; bespoke user-info card + inline theme/logout → AppHeader user-menu with dropdown. tsc clean · `npm run build` green (10.18s). **Direct per-page migration paused** — producing 71× inlined shell boilerplate OR creating `<PageShell>` without a Claude Design spec both cost more than they buy; S4 Detail session will ship the canonical `<PageShell>` primitive, and the shim → primitive cutover becomes a 5-minute find-replace (`DashboardHeader` → `PageShell`). Until then, the shim is the effective PageShell. Docs updated: SYSTEM_STATE · SYSTEM_OVERVIEW · RECENT_CHANGES · W07 card · DESIGN_CATALOG (AppHeader adoption 1/80 → 72/80 indirect via shim). Checklist of 71 adopter files preserved in this conversation for the eventual direct-migration sweep.

**2026-04-19 (eod+15i)** — **W07 Phase 2 infra batch shipped (items 4/5/6/7).** Four independent, no-design-needed Phase 2 chores closed in one pass: (1) **typed Supabase client** at [`src/lib/supabase/typed-client.ts`](../../src/lib/supabase/typed-client.ts) — `fetchPage` · `fetchOne` · `fetchDropdown` · `fetchCount` wrap the raw client and force `.range` / `.single` / `.limit(5000)` / `{ head: true }` at the type level; `table` name typed from `Database['public']['Tables']`, `build` callback for `.eq`/`.ilike`/`.order`; re-exports raw `supabase` as `db` for auth/storage/RPC. (2) **4 cell helpers** at [`src/components/primitives/shell/cells/`](../../src/components/primitives/shell/cells/) — `<DateCell>` + `<DateTimeCell>` (wrap `timezoneUtils.formatDisplayDateLong/LongTime`), `<CurrencyCell>` (right-aligned tabular-nums · negatives → red-700), `<NumberCell>` (configurable decimals + unit suffix). Drop into any `ColumnDef.cell`. (3) **`<SearchInput>`** at [`src/components/primitives/shell/SearchInput.tsx`](../../src/components/primitives/shell/SearchInput.tsx) — cell-level search atom extracted from `<FilterBar>` (sizes sm/md · optional ⌘K hint · clear button); FilterBar refactored to compose it — zero API break. (4) **Vitest runner wired** — vitest + @testing-library/react + jest-dom + jsdom + user-event installed; [`vitest.config.ts`](../../vitest.config.ts) + [`src/test/setup.ts`](../../src/test/setup.ts) + `npm test`/`test:run`/`test:ui` scripts. The 4 W07 Phase 1 primitive specs (EmptyState · ConfirmDialog · FormShell · DataTable) that were written-ahead-of-runner now run green — 22/22 tests pass, 4 files pass (pre-existing unrelated `validation-error-handling.test.tsx` has 6 failures — out of scope). Docs updated: [primitives/CONTEXT.md](../../src/components/primitives/CONTEXT.md) (count 28→35 + 10 new rows in replacement table) · [src/lib/README.md](../../src/lib/README.md) (typed-client section) · [DESIGN_CATALOG.md](DESIGN_CATALOG.md) (S-shell row → 11+4, lock list → 36 primitives + infra) · [W07 card](workflows/W07_SHARED_PRIMITIVES.md) (3 chores flipped ✅) · [.claude/rules/universal-components.md](../../.claude/rules/universal-components.md) (10 new replacement rows) · [TIMEZONE_POLICY.md](../../docs/01-system-architecture/TIMEZONE_POLICY.md) + [SEARCHABLE_SELECT_COMPONENT.md](../../docs/01-system-architecture/SEARCHABLE_SELECT_COMPONENT.md) + [react-query-cache/CONTEXT.md](../../docs/01-system-architecture/react-query-cache/CONTEXT.md) cross-linked. `npx tsc --noEmit` clean on all new files · `npm run build` green (10.44s). **W07 Phase 2 status**: items 1-7 closed (Button · AppHeader · Drawer shipped earlier; typed wrapper · test runner · date/currency cells · search-bar cell today). Items 8-11 (KpiTile · Chart · Timeline · Stepper) remain blocked on S3-KPI / S4 Detail / S6 Form Claude Design sessions.

**2026-04-19 (eod+15h)** — **S2 overlays visually verified live · Toaster spec-match via `toast.custom(AppBaseToast)` · `/design-lab/overlays` permanent regression lab · `/use-primitives` skill landed.** User flagged that toast "looked different from the spec". Root cause: sonner's default layout can't be fully styled via classNames (can't replace the built-in icon with spec's 18px glyph circle, can't pin XClose). Fix: pivoted toastHelper from `toast.success()` → `toast.custom((id) => <AppBaseToast …/>)`. New `AppBaseToast` component + bumped to `bg-white/95 backdrop-blur-[16px] saturate-160%` per user "too transparent" feedback. 8 other overlays got pixel tweaks (Alert bg 88%, Modal xl 560, Drawer 2 pulse rings, Popover +withArrow, Tooltip rounded-5, Dropdown/Context items rounded-5, SMS panel 340). Nested `<button>` inside `<button>` in SMSBadge fixed. New `/design-lab/overlays` route ([`OverlaysLabPage.tsx`](../../src/features/design-lab/overlays/OverlaysLabPage.tsx)) renders all 9 overlays + list atoms in default+open state — one URL for regression checks; linked from `/design-lab` top bar. All 9 verified vs `design/session-02-overlays/export/appbase/project/Overlay System.html` — matches spec. Toaster root-mounted; `ui/sonner` is a shim → 100+ legacy callers auto-benefit. `CommandPalette` primitive added (overlays 9→10, total 52). New governance layer: [UNIVERSAL_COMPONENTS.md](UNIVERSAL_COMPONENTS.md) rulebook + [.claude/rules/universal-components.md](../../.claude/rules/universal-components.md) auto-loader + [.claude/commands/use-primitives.md](../../.claude/commands/use-primitives.md) slash command (3 modes: USE/EDIT/CREATE). tsc + build green. **S-shell gap status**: AppHeader 1/80 (79 W09 pending) · Button/Chip/FilterBar/FloatingCTA/LoadingSkeleton/ErrorState/NoResultsState all built + spec-verified but 0 production adoption beyond `/dashboard` — W09 rollout is what closes adoption.

**2026-04-19 (eod+15)** — **@p0 suite grew 13 → 16 · W21 formally CLOSED · W15.02 Part A shipped.** Three new @p0 specs landed green on desktop + mobile (commit `95e2959`): WF-0099 Cache-staleness (seeds quotation · edit project_name · back · asserts fresh list without refresh — closes W21 formally), WF-0013 Approve quotation (seeds SENT · clicks Accept · DB status_id/accepted_at/logs/comments verified), WF-0004 Create client (promoted existing WF-0213 spec to @p0). P0 score 3/19 → 6/19. Ledger all-green 85 → 88. CI run 24624189859 green. **W15.02 Part A 🟢 shipped (commit `b37e301`)** — plugged real PII leak on `public.worker_applications` (`SELECT: true` → `has_module('HR Applications') OR is_admin()`). Migration `20260419_165635_w15_02_worker_applications_rls.sql` + SECURITY DEFINER helper `w15_02_policy_audit` for the seatbelt. Seatbelt `tests/workflows/rls/worker-applications-rls.spec.ts @p0` green. Permissive-policy count 14 → 13. **Part B deferred to post-W17** (needs multi-role test users for regressive tightenings on worker_leave_balance + worker_ot + salary/pay_slips worker-self-read). W21 formal CLOSED now that the cache-staleness Playwright gate is green. Mobile-safari 2-flake (WF-0099 + WF-0019 happy) ordering-dependent — pre-existing, passes in isolation, retries handle it. Earlier eod+14 context: W08 Session 2 Overlays + DESIGN_CATALOG 45 primitives + W15.04/W15.05 🟢 (see RECENT_CHANGES).

**2026-04-19 (eod+14)** — **W08 Session 2 Overlays shipped + DESIGN_CATALOG → 45 primitives + design-first workflow locked.** Session 2 (Overlay System) designed in Claude Design + 9 primitives built at `src/components/primitives/overlays/` (Modal · Drawer · Popover · Tooltip · DropdownMenu · ContextMenu · Alert · Toaster · SearchableMultiSelect + shared Kbd). Commit `a9cd88a`. tsc + build clean (CSS 277 → 300 kB). Not adopted — user gated on full-set design approval. DESIGN_CATALOG extended: 38 → 45 primitives across 7 groups (added G. Spatial + IntegrationCard + ManualOverrideToggle + WorkflowProgressBar + ProgressCard + MapCanvas + SpatialPicker + DrawingModal). AppHeader scope expanded w/ breadcrumb + user menu + view-as-user impersonation (via `agent@example.com` demo accounts for UI/UX + future Playwright role coverage). 3 new session folders staged (S10 Progress · S11 Spatial · S12 Integration). Commit `c6b7db4`. **Design sessions active in Claude Design**: S-shell (post-ErrorState-tweak iteration — now uses Geist Pixel Grid matching 404 aesthetic); S3 Dashboard (re-prompted to target actual module-launcher at `/dashboard`, not generic analytics). **User rule locked** (memory + state): design-first workflow — no primitive builds until user approves full design set, no W09 migration until post-approval. Also closed today: **W15.04 🟢** (projects + quotations RLS · ledger 16 → 14, migration `20260419_160656`) + **W15.05 🟢** (ops + logs RLS · 12 permissive policies dropped · ledger 14 → 2; only W15.02 payroll remains for G4 `rls_restored`).

**2026-04-19 (eod+12)** — **W12.03 → 🟢 DONE · W12.03b closes 4 live action-level RBAC gates via capabilities.** Swapped `ProjectDetailPage.isAdmin` → `hasCapability('finance')` (existing cap) · `PeopleManagement.isSuperAdmin` → `hasCapability('change_user_role')` (prop renamed `canChangeRole`) · `XeroSettingsPage.isSuperAdmin` → `hasCapability('manage_integrations')` (var renamed `canManageConnection`) · `OTEditModeToggle.canEdit` → `hasCapability('edit_ot_entries')`. Migration `w12_03b_add_ui_capabilities` applied via Supabase MCP — 3 new capability slugs × 5 role rows. AuthContext extended to load `rls_capabilities` by role in `checkAuth()` + `startImpersonation()` and expose `capabilities: string[]` + `hasCapability(cap): boolean` (no signature break). tsc clean · Playwright touched modules (peoplemanagement + xero-settings + ot-calculator + projectlist + auth + smoke) = 13/13 post-retry (only flake WF-0314 transient login). 2 Index.tsx sites remain deferred to W09 auth migration (not blocking). Server/UI drift flag: `users_mgmt_write` RLS still allows both `management` + `super_admin` to write users, but UI now gates role changes to super_admin via `change_user_role` — tighten server in W15.01 follow-up. W14 permissive-policy ledger unchanged (UI-only sweep). Audit trail in [research/W12_03_SITES.md](research/W12_03_SITES.md).

**2026-04-19 (eod+11)** — **Primary CTA LOCKED at slate-700 · LOCKED_PICKS v3.1 amended.** User reverted the short-lived eod+8 slate-500 override (white-on-slate-500 contrast 4.14:1 — borderline WCAG AA) to **slate-700 `#334155` (hsl 215 25% 27%)**. White-on-slate-700 contrast = **10.7:1 WCAG AAA**. Code was already ahead: `src/index.css` shipped with `--primary: 215 25% 27%` and `--cta-primary-bg: #334155` marked `USER FINAL PICK 2026-04-19`. Reconciled state docs: (a) [LOCKED_PICKS.md](LOCKED_PICKS.md) v3.1 amended — primary CTA row now reads slate-700 + 10.7:1 AAA; Open-picks "exact zinc-700 shade" struck through as RESOLVED; W07/W08 follow-up lines updated. (b) [W08 card](workflows/W08_DESIGN_SYSTEM.md) Phase 2 decisions line switches from slate-500 override to slate-700 final; pre-flight checklist item ✅. (c) SYSTEM_STATE + SYSTEM_OVERVIEW W08 rows reconciled to slate-700. This resolves the Q-W08-a CTA-color strand that Phase 2 notes flagged as borderline. No visual regression — the W12.04 manual check (eod+9) was already against slate-700, since the slate-500 experiment had been reverted in index.css before that run.

**2026-04-19 (eod+10)** — **W08 Phase 3 planned via Claude Design · W07 Phase 2 spec-gated on it.** Wrote new planning doc [CLAUDE_DESIGN_GAME_PLAN.md](CLAUDE_DESIGN_GAME_PLAN.md) (351 lines) — the durable map for using Anthropic's Claude Design research preview ([claude.ai/design](https://claude.ai/design), launched 2026-04-17) to lock AppBase's visual language archetype-by-archetype. 6 sessions covering all 80 pages across 6 archetypes (List/Table 26 routes · Dashboard 11 · Detail 12 · Form 8 · Settings 7 · Tool 6 + 4 low-prio Auth + 3 review-hybrids + 1 Inbox + 2 internal). Per session: 12 state variants (6 states × light/dark), handoff bundle output (screenshots + decisions.md + optional HTML/CSS export) staged at `docs/99-refactor/_system/design/session-NN-<archetype>/`. My implementation loop per session: W08 token refinements + W07 primitive(s) built/refined to spec + 2 W09 proof-module migrations. Critical dependency table documented: Session 1 List/Table unblocks W07 `<AppHeader>` glass + `<Button variant=primary>` rebuild + new `<FilterBar>` + `<DataTable>` refine · Session 2 Dashboard unblocks `<KpiTile>` + `<Chart>` · Session 3 Detail unblocks `<Timeline>` · Session 4 Form unblocks `<Stepper>` + `<MobileDrawer>` · Sessions 5+6 are composition-only. After Session 4 W07 Phase 2 primitive-build work is effectively complete. W08 card reframed: Phase 3 scope flipped from "motion primitives + /design-system reference page" → "Claude Design archetype sessions"; old Phase 3 work becomes Phase 4 follow-up (non-blocking for W09). W07 card reopened: Phase 2 explicitly 🔴 BLOCKED on W08 Phase 3, with per-primitive dependency table. DOCUMENTATION_INDEX.md registers [CLAUDE_DESIGN_GAME_PLAN.md](CLAUDE_DESIGN_GAME_PLAN.md). Handoff protocol documented: claude.ai/design URLs are auth-gated (I can't WebFetch), so handoff goes through local staging folder + pasted screenshots + text. Pre-flight for Session 1: user to (a) confirm slate-500 stays vs flip to slate-600 for WCAG AA (contrast 4.14 vs 7.1:1), (b) run curated-bundle rsync producing `~/Desktop/appbase-design-bundle/` with 11 key paths (src/index.css · src/lib/design/ · src/components/ui/ · src/features/design-lab/ · src/features/staff-management/ · reference pages · LOCKED_PICKS.md · game plan doc), (c) upload bundle + fonts + company-blurb to claude.ai/design setup form, (d) say "Session 1 setup complete" → I stage `session-01-list-table/README.md` with exact prompt. Timeline estimate: ~4-5h user + ~10-12h me + ~10-12 review rounds across the 6 sessions. Then W09 mass migration consumes remaining 68 pages against archetype patterns.

**2026-04-19 (eod+9)** — **W12.04 → 🟢 CLOSED · Sweep 3 Timezone.** 332/352 raw `format()` calls (94%) + all 9 `parseISO` sites swapped across 155 files in 10 batches (commits `58fd0ce → 01919bb`). Wrapper-first strategy: batch 0 extended `timezoneUtils.ts` with 8 new SGT-safe wrappers (`formatDisplayDateLong/TimeLong`, `Slashed/TimeSlashed`, `US/TimeUS`, `formatMonthYear`, `parseFromDatabase`) + smoke test `scripts/verify_timezoneUtils.mjs` (8/8 pass); this lifted auto-swap from 45% → 94% before the codemod ran. Batch 1b collapsed `useWorkerOTSummary`'s `toLocaleString('en-US', { timeZone: 'Asia/Singapore' })` wall-clock hack (was coincidentally correct on UTC+8 browsers only). 20 NEEDS-HUMAN tail documented: shadcn PPP/LLL date-pickers (4+6), payslip PDF `dd-MMM-yy` (3), filename slugs `MMM_yyyy` (2), local-tz form inputs in `datetime-picker.tsx` (2), tooltip weekday + short-year cosmetic (3). Rule [.claude/rules/timezone.md](../../../.claude/rules/timezone.md) updated: (a) expanded Key-Utilities table with the 8 new wrappers; (b) allowlist for raw `date-fns` arithmetic (`startOfMonth`, `addMonths`, `eachDayOfInterval`, etc.) and comparison (`isAfter`, `isBefore`, `isValid`, etc.) — "returns a string → wrapper; returns Date/number/boolean → raw OK"; (c) NEEDS-HUMAN list for local-tz form widgets. Every commit `tsc --noEmit --incremental` clean under W22 pre-commit gate (HEAD baseline 791 pre-existing errors, delta 0 per batch — verified via normalized diff). **Seatbelt**: Playwright @p0 subset 6/6 green (chromium-desktop + mobile-safari); touched-module specs 55/56 green (sole fail = known WF-0019 `confirm-assign-ai` NAS flake, already dropped from @p0 in commit `4fe630f` — unrelated). **Manual visual spot-check**: /quotations · /payslip · /ot-calculator · /generalworks · /emaillogs — all dates render correctly in SGT (April 19); no drift observed. Display-variant wrappers flagged `TODO W08: consolidate` pending W08's canonical-style pick. Sub-flag `sweep_3_timezone_done` ✅ (feeds G4). Triage: [research/W12_04_TRIAGE.md](research/W12_04_TRIAGE.md) · Card: [workflows/W12.04_SWEEP_3_TIMEZONE.md](workflows/W12.04_SWEEP_3_TIMEZONE.md).

**2026-04-19 (eod+8)** — **W08 → 🟡 IN PROGRESS · Phase 2 shipped.** App-wide activation layer lands on top of Phase 1's token foundation. Fonts globalised: `@fontsource/roboto` (400/500/700) + `@fontsource/geist-mono` (400/500/700) moved from `FontTestPage.tsx` to `src/main.tsx` — every page loads them; `--font-sans` / `--font-mono` / `--font-pixel` added to `@theme`; `body { font-family: var(--font-sans) }` + `code/kbd/pre/samp { font-family: var(--font-mono) }` in base layer. **Shadcn semantic remap** (one change, 400+ components instantly): `--primary` → **slate-500 hsl(215 16% 47%)** (⚠️ user override 2026-04-19 of LOCKED_PICKS v3.1 zinc-700 — softer grey; white-on-slate-500 contrast 4.14:1, borderline WCAG AA; bump to slate-600 in one line if flagged), `--destructive` → red-700 hsl(0 74% 42%), `--ring` → red-700; `.dark` flips primary to slate-100. **ThemeProvider hoist**: now at `src/App.tsx` root wrapping `RouterProvider`; `useEffect` flips `document.documentElement.classList.toggle('dark')` — not a wrapper, so every page respects the toggle including non-DashboardHeader routes. `src/lib/design/ThemeToggle.tsx` (new) cycles system → light → dark → system; placed twice in `DashboardHeader` (desktop sidebar + mobile bar). **Base-layer tokens**: h1-h6 heading scale with tight leading + negative letter-spacing (Linear/Vercel-inspired), `a { color: var(--brand-red) }` links, `:focus-visible { outline: 2px solid var(--brand-red) }` brand ring. **7 shadcn `ui/*` retrofits**: dialog `bg-white` → `bg-background`; count-badge `text-white bg-red-500` → `text-destructive-foreground bg-destructive`; bulk sed across loading-states, available-people-select, people-select, accessible-components, project-select-with-validation (swaps `text-gray-*`/`bg-gray-*`/`border-gray-*`/`hover:bg-gray-*`/`bg-white/80` to semantic tokens). Kept as-is: sheet + alert-dialog (`bg-black/80` is standard overlay), chart (hex literals are chart v2 scope), accessible-components skip-link (`bg-blue-600 text-white` for a11y-specific contrast anchor). **Violation audit [research/W08_PHASE_2_VIOLATIONS.md](research/W08_PHASE_2_VIOLATIONS.md)**: full rg scan across `src/features` + `src/components` (ex-`ui/*`) catalogues **5,556 hardcoded sites across ~500 files** — 289 files × `bg-(white|black|gray-*)` (1,013 occ) · 338 × `text-*` (2,752 occ) · 185 × `border-gray-*` (511 occ) · 305 × `rounded-*` (862 occ) · 40 × `shadow-*` (65 occ) · 119 × `transition-*` for Q-W08-b RESERVED review (205 occ) · 17+2 × hex/rgb literals (148 occ). Top-20 offenders: ProjectViewTab (70), PerformanceReviewStats (63), PlanPurchaseEditMode (62), CompanyDetailView (62), NCEEditMode (53), OTDetailsTab (52), XeroInvoicesTab (49), quotation/enhanced/* (47+45), etc. — concentrate in project-management + quotation + payment-management + performance-review domains. Forecast: W07 primitives absorb ~1,500–2,000 (35%); W09 per-module eats ~2,400 residue. **Verification**: `tsc --noEmit` ✅, `npm run build` ✅ (CSS 270.90 → 271.74 kB, +1 kB for Phase 2 tokens + base layer), Playwright seatbelt **16/16 green** on fresh dev server (auth/login-admin + smoke/all-modules + clientprofiles × chromium-desktop). Phase 3 (motion primitives + `/design-system` reference page) deferred — NOT blocking W09. Toaster inline style fixed to use `hsl(var(--background))` tokens so dark mode renders correctly. **W08 stays 🟡 IN PROGRESS** until W09 consumes the violation audit. Full notes: [research/W08_PHASE_2_NOTES.md](research/W08_PHASE_2_NOTES.md).

**2026-04-19 (eod+7)** — **W12.03 post-W08-Phase-1 verification · 5/6 @p0 green.** Revalidated the 4 `SKIP_E2E=1` commits (`200968b` · `704fd0c` · `c500f78` · `8a4ec3f`) once W08 Phase 1 settled in-tree. tsc ✅ · build ✅ (12.90s, CSS 271 kB / gzip 56 kB) · dev server `/login` 200 with Tailwind v4 HMR clean · Playwright `@p0` WF-0001 + WF-0012 green on both projects + WF-9000 chromium-desktop green · only fail WF-9000 **mobile-safari** at different routes each run (`/serviceslist: could not connect` → `/ot-calculator: body nearly empty` on retry). Not W12.03's surface (orphan-only); likely mobile-safari + Tailwind v4 HMR while W08 Phase 2 components remain uncommitted. Flag for W22 GH-Actions triage. Full entry in [RECENT_CHANGES.md](RECENT_CHANGES.md).

**2026-04-19 (eod+6)** — **W08 → 🟡 IN PROGRESS · Phase 1 shipped.** Tailwind v3.4.11 → v4.2.2 migrated manually (codemod skipped — its utility-rename sweep is Phase 2 scope per the plan guardrail). CSS-first config: `tailwind.config.ts` deleted; all tokens now in `src/index.css` via `@import "tailwindcss"` + `@plugin "tailwindcss-animate"` + `@plugin "@tailwindcss/typography"` + `@custom-variant dark (&:where(.dark, .dark *))` + `@theme { … }` block with shadcn HSL compat (preserves 400+ existing call sites) + Radix zinc/red 1–12 scales + full LOCKED_PICKS v1/v2/v3 token set (Card, DataTable, KpiTile, Drawer, Stepper, Timeline, Charts + CTA, Glass, Mobile patterns) + Q-W08-b motion tokens (`--motion-duration-instant/quick/smooth` + `--motion-ease-out-expo/spring`). `.dark` overrides wired for every colour token. `src/lib/design/tokens.ts` TypeScript mirror (11 deep-import groups per Q-W07-b). `/design-lab/fonts` 3-way comparison lab (Roboto via `@fontsource/roboto`, Geist Mono via `@fontsource/geist-mono`, Geist Pixel Square self-hosted from `vercel/geist-pixel-font` — not on npm yet). Scoped `ThemeProvider` at `src/lib/design/ThemeProvider.tsx` (light/dark/system, persisted to `localStorage:w08:theme:v1`). `DashboardHeader` gained a "Font lab · Q-W08-a" link inside `/design-lab`. `DESIGN_SYSTEM.md` formally deprecated — banner at top, status flipped 🟢 → 🔴 in `DOCUMENTATION_INDEX.md`. Verification: `tsc --noEmit` clean; `npm run build` clean (CSS 230 → 270 kB, gzip 56 kB); **Playwright seatbelt 34/34 green** on fresh dev server (auth + quotation create + smoke + clientprofiles × chromium-desktop + mobile-safari). **Phase 2 scope (blocks W09)**: user locks Q-W08-a via the lab, then (a) apply picked font app-wide as body + display; (b) audit ~200 hardcoded colour sites → tokens or W09 per-module; (c) wire app-wide theme toggle in header; (d) `src/components/motion/` primitives; (e) `/design-system` reference page. Full Phase 1 notes + gotchas: [research/W08_PHASE_1_NOTES.md](research/W08_PHASE_1_NOTES.md).

**2026-04-19 (eod+5)** — **W12.03 → 🟡 PARTIAL · RBAC sweep 6/8.** Orphan admin pages (AdminCompanies/Projects/Workers) swapped to `MODULE_PATH = '/admin'` — zero behavior change (pages unreachable today). 2 Index.tsx sites deferred to W09 auth migration (uses `useState` + RPC, not `useAuth()`). 8 additional `user?.role` sites spillover to W12.03b (4 redundant `enabled:` gates + 4 live action-level flags). tsc clean; Playwright deferred (W08 dev-server WIP; orphan pages are zero-impact). Triage in [research/W12_03_SITES.md](research/W12_03_SITES.md). Landed via `ec756c7`.

**2026-04-19 (eod+3)** — **W17 → 🟢 CLOSED · X6 ✅ COMMITTED.** All 7 real-choice slots locked + 3 cross-cutting patterns (CTA · glass · mobile data-table). DAG flag `components_chosen` SET. W07 + W08 both unblocked. DESIGN_SYSTEM.md formally deprecated; W08 writes the token-spec replacement. Full handoff in [LOCKED_PICKS.md](LOCKED_PICKS.md). Full history in [RECENT_CHANGES.md](RECENT_CHANGES.md).

**2026-04-19 (eod+2)** — **W19 → 🟢 CLOSED.** All 3 tracks of MWP context architecture shipped one-pass.
- **Track 2 (TOKEN_BUDGET)**: `docs/99-meta/TOKEN_BUDGET.md` (5,600 chars / 85 lines). Per-file ceilings: root CLAUDE.md 3,200 · feature CONTEXT.md 1,600 · category CONTEXT.md 2,400 · guide 8,000 · feature doc 12,000 · reference doc 15,000 · decisions/lessons 50 entries. 6-row exemption table (root CLAUDE.md · DOCUMENTATION_INDEX · SYSTEM_OVERVIEW · SYSTEM_STATE · WORKFLOW_LEDGER · RECENT_CHANGES) each justified. Registered in DOCUMENTATION_INDEX.md (99-meta count 3 → 4). `/check-docs` enforcement deferred to W22 per Q-W19-e (CI-only, not pre-commit).
- **Track 1 (CONTEXT.md)**: 6 new feature workspaces + 8 verified-existing workspaces, all within budget. New: `src/features/refactor-dashboard/` · `src/features/design-lab/` · `src/features/serviceslist/` · `src/components/` · `src/hooks/` · `src/utils/` · `supabase/migrations/` · `supabase/functions/` · `.claude/commands/` · `.claude/rules/` · `tests/` · `tests/workflows/` · `tests/pom/` · `tests/runners/`. `src/CONTEXT.md` updated to add `features/` folder.
- **Track 3 (decisions/lessons)**: 3 workspaces seeded with verified institutional history. `src/features/refactor-dashboard/`: 3 decisions + 1 lesson. `supabase/migrations/`: 3 decisions + 2 lessons. `tests/workflows/`: 3 decisions + 3 lessons. Skipped 4th candidate (`src/features/quotation/` doesn't exist — quotation lives under `src/components/quotation/`; per Q-W19-c don't force-seed empty workspaces).
- **Root CLAUDE.md**: 2 lines added — *"Before working in a workspace, check for `decisions.md` / `lessons.md` there"* + *"File rules + budgets live in [docs/99-meta/TOKEN_BUDGET.md]"*. Trimmed Structure + MCP sections to compensate; final 3,533 chars (333 over 3,200 ceiling — exempted in TOKEN_BUDGET.md).
- **Sets flag**: `context_refreshed` ✅. G2 merge gate (S2 → S3) now: `seatbelt ∧ wf_map ∧ ci_enforced ∧ drift_on ∧ components_chosen ∧ context_refreshed ∧ cache_fixed` — `context_refreshed` ✅ + `cache_fixed` ✅ + `components_chosen` 🟡 (3/7 slots locked) + `seatbelt`/`wf_map` 🟡 (W03/W04) + `ci_enforced`/`drift_on` 🔴.
- **Follow-ups (non-blocking)**: (a) `/check-docs` skill should read TOKEN_BUDGET.md instead of hardcoded numbers; (b) parser-level schema check for WORKFLOW_LEDGER.md header row; (c) seed decisions/lessons for additional workspaces as W09 modules graduate.

**2026-04-19 (eod+1)** — **W21 → 🟢 IMPLEMENTATION SHIPPED.** All 4 tasks merged to main across the day: W21-1 `28dedce` (hardcoded keys → factory · 28 files · factory extended with 6 new top-level entries), W21-2 `f6b513f` (`invalidateDashboards()` wired into 17 source-entity mutation hooks — dashboard counts now refresh on every mutation), W21-3 `016219b` (4 new/extended hook files covering 11 mutations previously inline in InvoiceEmailRecipientsSection, ProgressClaimNASFolderCard, UserApprovalsTab, EditPerformanceDialog), W21-4 `e38d94e` (ESLint hardcoded-key rules promoted `warn` → `error`; new rule bans `useMutation` in `src/components/**`; react-query-cache/CONTEXT.md corrected — false "34/34 · 100% complete" claim killed). Card reconciled at `e1a8d02`. Formal 🟢 CLOSED still gated on W04 P0 cache-staleness Playwright test (the "edit → back → assert fresh without manual refresh" matrix). Also today: **W17 v1 picks locked** (Card = shadcnblocks-clean · DataTable = TanStack+Motion · KPITile = Tremor+NumberTicker; LOCKED_PICKS.md now lives with W07/W08 handoff notes); **v2 loaded** into `/design-lab` (Drawer · Stepper · Timeline · Charts + 2 new mockups) awaiting user picks. **W12.05 HITL triage complete** — 0 NEEDS-HUMAN remaining; final distribution 209 LEGACY-FIX + 22 PAGINATE + 18 ALREADY-OK + 1 DROPDOWN; card stays 🔴 for the fix-application phase.

**2026-04-19 (eod)** — **W15.03 → 🟢 CLOSED as no-op.** Live `pg_policies` scan of 11 client-domain tables (`client_*` + `customer_interactions`) found zero `ai_ro_select`/`USING(true)` holes — every table already capability-gated (`is_approved_user()` read + `can_manage_projects()` / `is_admin()` / `can_manage_quotations()` write). Matrix's W15.03 strategy ("add project_id filter") was aspirational tightening, out of W15's permissive-hole-closure scope. No migration applied, no Playwright run. Ledger unchanged at 16. If drafter row-scoping desired later, spawn W15.03b. W14 card updated with 16-hole owner breakdown (W15.02=2 · W15.04=2 · W15.05=12, + 4 intentional keeps). **Earlier today**: W15.01b shipped (identity-domain clean 4/4) · W03/W04/W09/W21 flips. Full history in [RECENT_CHANGES.md](RECENT_CHANGES.md).

**2026-04-19 (mid)** — **W09 → 🟡 pilot started · /serviceslist migrated to `src/features/`.** Seatbelt-first: 3 P1·T1 specs (WF-0276/0277/0278) green 6/6. `git mv` of `ServicesListPage.tsx` into `src/features/serviceslist/pages/`; tsc + build clean; seatbelt re-ran 6/6 green post-move. Recipe at [research/W09_RECIPE.md](research/W09_RECIPE.md). W21 reconciled 🟡 — commit `28dedce` shipped W21-1.

## Related
- [SYSTEM_OVERVIEW.md](SYSTEM_OVERVIEW.md) — the index
- [RECENT_CHANGES.md](RECENT_CHANGES.md) — full changelog
- [workflows/](workflows/) — per-sub-workflow cards
- [research/](research/) — evidence base
- Root `STATE.md` — not yet established; add entry when first system other than this exists
